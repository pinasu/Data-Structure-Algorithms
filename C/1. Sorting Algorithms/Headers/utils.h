#ifndef STD_H
#include "std.h"
#endif

/*
 * Function: swap
 * -------------------------
 * Prototype of swap to swap two elements in a given generic type array.
 *
 * array: the pointer to the generic array,
 * i: the index of the first element to be swapped,
 * j: the index of the second element to be swapped.
 *
 */
void swap(void** array, int i, int j);
